using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

/*
 * --------
 * class Unit
 * 
 * void moveOn (Hexagon hexagon)
 * {
 *     this.deplacementPoints -= this.costOfMovementOn(hexagon.;
 *     if (hexagon.isOccuped() )
 *     {
 *          bool ennemyDestroyed = attack(hexagon.selectBestUnit()));
 *          if (ennemyDestroyed)
 *          {
 *              hexagon.addUnit(this);
 *          }
 *     }
 * }
 * 
 * // returns true if defender is destroyed
 * bool attack(Unit defender)
 * {
 *      int fightNumber = math.random(entre 3 et 2+max(this.lifePoint, defender.lifePoint));
 *      while (fightNumber > 0)
 *      {
 *         double chanceOfVictory = 0.5*(this.getAttack() / defender.getDefense())
 *         double godDecision = math.random(entre 0 et 1);
 *         if (chanceOfVictory > godDecision)
 *         // attacker wins
 *          {
 *              defender.lifePoints -= this.attack * (this.lifePoint / this.lifeMax)
 *              if (defender.lifePoints < 0)
 *              {
 *                  fightNumber = 0;
 *                  defender.destroy();
 *                  return true;
 *              } 
 *          }
 *          else
 *          // attacker looses
 *          {
 *              attacker.lifePoints -= defender.defense * (defender.lifePoints / defender.lifeMax);
 *              if (this.lifePoints < 0)
 *              {
 *                  fightNumber = 0;
 *                  this.destroy(); // ??
 *              }
 *          }
 *      }
 *      return false;
 * }



 * 
 * 
 * 
 * 
 * 
 * 

 
 
 * 
 * 
 * 
 * 
 * 
 * 
 * -----------
 * class Hexagon
 * 

*/