using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

/*
class Unit
 * 
void deplacer (Hexagon hexagon)
{
    this.deplacementPoint--;
 * if (hexagon.isOccuped() )
 * {
 *      attack(hexagon.selectBestUnit());
 * }
}
 * 
 * void attack(Unit defender)
 * {
 *      int fightNumber = math.random(machin);
 *      while (fightNumber > 0 || (this.lifePoint > 0 and defender.lifePoint > 0))
 *      {
 *         COOMMMMMMBAAAATTT
 *      }
 * }

 * void selectBestUnit()
 * {
 *      
 * }
*/